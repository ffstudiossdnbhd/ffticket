<UserControl
    x:Class="FFTicket.Desktop.Views.TimeoutView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource Card}">
            <Grid>
                <StackPanel>
                    <TextBlock FontSize="22" FontWeight="SemiBold" Text="Account Timeouts" />
                    <TextBlock Margin="0,3,0,0" Foreground="{ThemeResource TextSecondaryBrush}" Text="Timeouts block new sign-ins immediately and end active access after one minute." />
                </StackPanel>
                <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center" Orientation="Horizontal" Spacing="10">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Foreground="{ThemeResource SuccessTextBrush}" Text="{Binding SuccessMessage}" />
                        <TextBlock Foreground="{ThemeResource ErrorTextBrush}" Text="{Binding ErrorMessage}" />
                    </StackPanel>
                    <Button Style="{StaticResource IconButton}" Command="{Binding RefreshCommand}" ToolTipService.ToolTip="Refresh timeout status">
                        <FontIcon Glyph="&#xE72C;" />
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="0">
            <controls:DataGrid
                AutoGenerateColumns="False"
                CanUserReorderColumns="True"
                CanUserResizeColumns="True"
                GridLinesVisibility="None"
                ItemsSource="{Binding Users}"
                RowHeight="52"
                SelectedItem="{Binding SelectedUser, Mode=TwoWay}">
                <controls:DataGrid.Columns>
                    <controls:DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
                    <controls:DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="110" />
                    <controls:DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="260" />
                    <controls:DataGridTextColumn Header="Release (MYT)" Binding="{Binding ReleaseAtMyt}" Width="170" />
                </controls:DataGrid.Columns>
            </controls:DataGrid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource Card}">
            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="190" />
                    <ColumnDefinition Width="150" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Spacing="3">
                    <TextBlock FontSize="17" FontWeight="SemiBold" Text="Selected User" />
                    <TextBlock Foreground="{ThemeResource TextSecondaryBrush}" Text="{Binding SelectedUser.Name}" />
                </StackPanel>
                <CalendarDatePicker
                    Grid.Column="1"
                    Date="{Binding ReleaseDate, Mode=TwoWay}"
                    DateFormat="{}{day.integer}/{month.integer}/{year.full}"
                    Header="Release date (MYT)"
                    PlaceholderText="Select date" />
                <TimePicker
                    Grid.Column="2"
                    ClockIdentifier="24HourClock"
                    Header="Release time (MYT)"
                    Time="{Binding ReleaseTime, Mode=TwoWay}" />
                <StackPanel Grid.Column="3" VerticalAlignment="Bottom" Orientation="Horizontal" Spacing="8">
                    <Button Command="{Binding StartOrUpdateCommand}" Content="Start / Update Timeout" IsEnabled="{Binding SelectedUser.CanTimeout}" />
                    <Button Style="{StaticResource SecondaryButton}" Command="{Binding ReleaseCommand}" Content="Release Now" IsEnabled="{Binding SelectedUser.CanTimeout}" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
