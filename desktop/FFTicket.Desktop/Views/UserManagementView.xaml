<UserControl
    x:Class="FFTicket.Desktop.Views.UserManagementView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource Card}">
            <Grid>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock FontSize="22" FontWeight="SemiBold" Text="User Management" />
                    <TextBlock
                        Margin="0,3,0,0"
                        Foreground="{ThemeResource TextSecondaryBrush}"
                        Text="Add accounts and manage existing user access." />
                </StackPanel>
                <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center" Orientation="Horizontal" Spacing="10">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Foreground="{ThemeResource SuccessTextBrush}" Text="{Binding SuccessMessage}" />
                        <TextBlock Foreground="{ThemeResource ErrorTextBrush}" Text="{Binding ErrorMessage}" />
                    </StackPanel>
                    <Button Style="{StaticResource IconButton}" Command="{Binding RefreshCommand}" ToolTipService.ToolTip="Refresh users">
                        <FontIcon Glyph="&#xE72C;" />
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource Card}">
            <StackPanel Spacing="12">
                <StackPanel>
                    <TextBlock FontSize="17" FontWeight="SemiBold" Text="Add User" />
                    <TextBlock Foreground="{ThemeResource TextSecondaryBrush}" Text="Create a staff, IT staff, or administrator account." />
                </StackPanel>
                <Grid x:Name="AddUserFields" ColumnSpacing="8" RowSpacing="8">
                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroup>
                            <VisualState x:Name="CompactAddUser" />
                            <VisualState x:Name="WideAddUser">
                                <VisualState.StateTriggers>
                                    <AdaptiveTrigger MinWindowWidth="1450" />
                                </VisualState.StateTriggers>
                                <VisualState.Setters>
                                    <Setter Target="AddColumn0.Width" Value="1.2*" />
                                    <Setter Target="AddColumn1.Width" Value="1.1*" />
                                    <Setter Target="AddColumn2.Width" Value="1.4*" />
                                    <Setter Target="AddColumn3.Width" Value="1.2*" />
                                    <Setter Target="AddColumn4.Width" Value="130" />
                                    <Setter Target="AddColumn5.Width" Value="Auto" />
                                    <Setter Target="NewNameInput.(Grid.Row)" Value="0" />
                                    <Setter Target="NewNameInput.(Grid.Column)" Value="0" />
                                    <Setter Target="NewNicknameInput.(Grid.Row)" Value="0" />
                                    <Setter Target="NewNicknameInput.(Grid.Column)" Value="1" />
                                    <Setter Target="NewEmailInput.(Grid.Row)" Value="0" />
                                    <Setter Target="NewEmailInput.(Grid.Column)" Value="2" />
                                    <Setter Target="NewPasswordInput.(Grid.Row)" Value="0" />
                                    <Setter Target="NewPasswordInput.(Grid.Column)" Value="3" />
                                    <Setter Target="NewRoleInput.(Grid.Row)" Value="0" />
                                    <Setter Target="NewRoleInput.(Grid.Column)" Value="4" />
                                    <Setter Target="AddUserButton.(Grid.Row)" Value="0" />
                                    <Setter Target="AddUserButton.(Grid.Column)" Value="5" />
                                </VisualState.Setters>
                            </VisualState>
                        </VisualStateGroup>
                    </VisualStateManager.VisualStateGroups>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition x:Name="AddColumn0" Width="*" />
                        <ColumnDefinition x:Name="AddColumn1" Width="*" />
                        <ColumnDefinition x:Name="AddColumn2" Width="0" />
                        <ColumnDefinition x:Name="AddColumn3" Width="0" />
                        <ColumnDefinition x:Name="AddColumn4" Width="0" />
                        <ColumnDefinition x:Name="AddColumn5" Width="0" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBox
                        x:Name="NewNameInput"
                        Grid.Row="0"
                        Grid.Column="0"
                        PlaceholderText="Name"
                        Text="{Binding NewName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBox
                        x:Name="NewNicknameInput"
                        Grid.Row="0"
                        Grid.Column="1"
                        PlaceholderText="Nickname"
                        Text="{Binding NewNickname, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBox
                        x:Name="NewEmailInput"
                        Grid.Row="1"
                        Grid.Column="0"
                        PlaceholderText="Email"
                        Text="{Binding NewEmail, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <PasswordBox
                        x:Name="NewPasswordInput"
                        Grid.Row="1"
                        Grid.Column="1"
                        PlaceholderText="Password optional" />
                    <ComboBox
                        x:Name="NewRoleInput"
                        Grid.Row="2"
                        Grid.Column="0"
                        ItemsSource="{Binding Roles}"
                        SelectedItem="{Binding NewRole, Mode=TwoWay}" />
                    <Button
                        x:Name="AddUserButton"
                        Grid.Row="2"
                        Grid.Column="1"
                        HorizontalAlignment="Stretch"
                        Click="AddUser_Click"
                        Content="Add User" />
                </Grid>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
            <controls:DataGrid
                AutoGenerateColumns="False"
                CanUserReorderColumns="True"
                CanUserResizeColumns="True"
                CanUserSortColumns="True"
                GridLinesVisibility="None"
                ItemsSource="{Binding Users}"
                RowHeight="50"
                SelectedItem="{Binding SelectedUser, Mode=TwoWay}">
                <controls:DataGrid.Columns>
                    <controls:DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60" IsReadOnly="True" />
                    <controls:DataGridTextColumn Header="Name" Binding="{Binding Name, Mode=TwoWay}" Width="*" />
                    <controls:DataGridTextColumn Header="Nickname" Binding="{Binding Nickname, Mode=TwoWay}" Width="*" />
                    <controls:DataGridTextColumn Header="Email" Binding="{Binding Email, Mode=TwoWay}" Width="1.4*" />
                    <controls:DataGridTextColumn Header="Role" Binding="{Binding Role, Mode=TwoWay}" Width="130" />
                    <controls:DataGridTextColumn Header="Created" Binding="{Binding CreatedAt}" Width="160" IsReadOnly="True" />
                </controls:DataGrid.Columns>
            </controls:DataGrid>
        </Border>

        <Border Grid.Row="3" Style="{StaticResource Card}">
            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Spacing="6">
                    <TextBlock FontSize="17" FontWeight="SemiBold" Text="Selected User" />
                    <TextBlock Foreground="{ThemeResource TextSecondaryBrush}" Text="Edit the selected row, optionally enter a new password, then save." />
                    <PasswordBox
                        x:Name="ResetPasswordInput"
                        MaxWidth="360"
                        HorizontalAlignment="Left"
                        PlaceholderText="New password (optional)" />
                </StackPanel>
                <StackPanel Grid.Column="1" VerticalAlignment="Bottom" Orientation="Horizontal" Spacing="8">
                    <Button
                        Click="SaveSelected_Click"
                        Content="Save Selected"
                        IsEnabled="{Binding SelectedUser, Converter={StaticResource NullToBooleanConverter}}" />
                    <Button
                        Style="{StaticResource SecondaryButton}"
                        Command="{Binding DeleteCommand}"
                        Content="Delete Selected"
                        IsEnabled="{Binding SelectedUser, Converter={StaticResource NullToBooleanConverter}}" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
