<UserControl
    x:Class="FFTicket.Desktop.Views.UserManagementView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource Card}">
            <Grid ColumnSpacing="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.2*" />
                    <ColumnDefinition Width="1.1*" />
                    <ColumnDefinition Width="1.4*" />
                    <ColumnDefinition Width="1.2*" />
                    <ColumnDefinition Width="130" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox PlaceholderText="Name" Text="{Binding NewName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <TextBox Grid.Column="1" PlaceholderText="Nickname" Text="{Binding NewNickname, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <TextBox Grid.Column="2" PlaceholderText="Email" Text="{Binding NewEmail, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <TextBox Grid.Column="3" PlaceholderText="Password optional" Text="{Binding NewPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <ComboBox Grid.Column="4" ItemsSource="{Binding Roles}" SelectedItem="{Binding NewRole, Mode=TwoWay}" />
                <Button Grid.Column="5" Command="{Binding CreateCommand}" Content="Add User" />
            </Grid>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="0">
            <controls:DataGrid
                AutoGenerateColumns="False"
                CanUserReorderColumns="True"
                CanUserResizeColumns="True"
                GridLinesVisibility="None"
                ItemsSource="{Binding Users}"
                RowHeight="50"
                SelectedItem="{Binding SelectedUser, Mode=TwoWay}">
                <controls:DataGrid.Columns>
                    <controls:DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60" IsReadOnly="True" />
                    <controls:DataGridTextColumn Header="Name" Binding="{Binding Name, Mode=TwoWay}" Width="*" />
                    <controls:DataGridTextColumn Header="Nickname" Binding="{Binding Nickname, Mode=TwoWay}" Width="*" />
                    <controls:DataGridTextColumn Header="Email" Binding="{Binding Email, Mode=TwoWay}" Width="1.4*" />
                    <controls:DataGridTextColumn Header="Role" Binding="{Binding Role, Mode=TwoWay}" Width="130" />
                    <controls:DataGridTextColumn Header="Created" Binding="{Binding CreatedAt}" Width="160" IsReadOnly="True" />
                </controls:DataGrid.Columns>
            </controls:DataGrid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource Card}">
            <Grid>
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <TextBlock VerticalAlignment="Center" Text="Reset Password" />
                    <TextBox
                        Width="220"
                        PlaceholderText="New password"
                        Text="{Binding ResetPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <Button
                        Command="{Binding UpdateCommand}"
                        Content="Save Selected"
                        IsEnabled="{Binding SelectedUser, Converter={StaticResource NullToBooleanConverter}}" />
                    <Button
                        Style="{StaticResource SecondaryButton}"
                        Command="{Binding DeleteCommand}"
                        Content="Delete Selected"
                        IsEnabled="{Binding SelectedUser, Converter={StaticResource NullToBooleanConverter}}" />
                    <Button Style="{StaticResource SecondaryButton}" Command="{Binding RefreshCommand}" Content="Refresh" />
                </StackPanel>
                <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock Foreground="{ThemeResource SuccessTextBrush}" Text="{Binding SuccessMessage}" />
                    <TextBlock Foreground="{ThemeResource ErrorTextBrush}" Text="{Binding ErrorMessage}" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
