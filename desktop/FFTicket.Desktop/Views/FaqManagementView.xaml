<UserControl
    x:Class="FFTicket.Desktop.Views.FaqManagementView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource Card}">
            <Grid>
                <StackPanel>
                    <TextBlock FontSize="22" FontWeight="SemiBold" Text="FAQ Management" />
                    <TextBlock Margin="0,3,0,0" Foreground="{ThemeResource TextSecondaryBrush}" Text="Publish plain-text answers available to every signed-in user." />
                </StackPanel>
                <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center" Orientation="Horizontal" Spacing="10">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Foreground="{ThemeResource SuccessTextBrush}" Text="{Binding SuccessMessage}" />
                        <TextBlock Foreground="{ThemeResource ErrorTextBrush}" Text="{Binding ErrorMessage}" />
                    </StackPanel>
                    <Button Style="{StaticResource IconButton}" Command="{Binding RefreshCommand}" ToolTipService.ToolTip="Refresh FAQs">
                        <FontIcon Glyph="&#xE72C;" />
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource Card}">
            <StackPanel Spacing="9">
                <TextBlock FontSize="17" FontWeight="SemiBold" Text="Add FAQ" />
                <TextBox PlaceholderText="Title" Text="{Binding NewTitle, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <TextBox MinHeight="80" AcceptsReturn="True" PlaceholderText="Description" Text="{Binding NewDescription, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" TextWrapping="Wrap" />
                <Button HorizontalAlignment="Left" Command="{Binding CreateCommand}" Content="Add FAQ" />
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
            <controls:DataGrid
                AutoGenerateColumns="False"
                CanUserReorderColumns="True"
                CanUserResizeColumns="True"
                GridLinesVisibility="None"
                ItemsSource="{Binding Faqs}"
                RowHeight="56"
                SelectedItem="{Binding SelectedFaq, Mode=TwoWay}">
                <controls:DataGrid.Columns>
                    <controls:DataGridTextColumn Header="ID" Binding="{Binding Id}" IsReadOnly="True" Width="64" />
                    <controls:DataGridTextColumn Header="Title" Binding="{Binding Title, Mode=TwoWay}" Width="1.1*" />
                    <controls:DataGridTextColumn Header="Description" Binding="{Binding Description, Mode=TwoWay}" Width="2*" />
                    <controls:DataGridTextColumn Header="Updated" Binding="{Binding UpdatedAt}" IsReadOnly="True" Width="160" />
                </controls:DataGrid.Columns>
            </controls:DataGrid>
        </Border>

        <Border Grid.Row="3" Style="{StaticResource Card}">
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal" Spacing="8">
                <Button Command="{Binding UpdateCommand}" Content="Save Selected" IsEnabled="{Binding SelectedFaq, Converter={StaticResource NullToBooleanConverter}}" />
                <Button Style="{StaticResource SecondaryButton}" Command="{Binding DeleteCommand}" Content="Delete Selected" IsEnabled="{Binding SelectedFaq, Converter={StaticResource NullToBooleanConverter}}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
